pragma solidity ^0.4.0;

import "./Unitarium.sol";
import "./Storage.sol";

/**
* @dev Rewards is a simple contract that holds UNITs in escrow from that a job is ordered until the payout happens.
**/
contract Rewards is Unitarium {

    function Rewards() public payable {

    }
}